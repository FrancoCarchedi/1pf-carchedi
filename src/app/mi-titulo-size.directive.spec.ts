import { MiTituloSizeDirective } from './mi-titulo-size.directive';

describe('MiTituloSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MiTituloSizeDirective();
    expect(directive).toBeTruthy();
  });
});
